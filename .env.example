# No environment variables are required by the current application.
# Put future local configuration in .env.local (ignored by Git).
# VITE_* variables are public browser configuration; never put secrets in them.
